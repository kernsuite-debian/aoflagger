#!/usr/bin/make -f
# -*- makefile -*-

#export DH_VERBOSE=1

CMAKE_FLAGS += -DCMAKE_SKIP_RPATH=ON -DDEB_BUILD_MULTIARCH=`dpkg-architecture -qDEB_BUILD_MULTIARCH`

%:
	dh $@ --parallel

override_dh_auto_configure:
	dh_auto_configure -- $(CMAKE_FLAGS)

override_dh_auto_test:
	true

